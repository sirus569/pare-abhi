@AGENTS.md

# Pare

Local-first personal finance app. Parses bank/CC PDF statements, categorizes transactions, shows spending trends, tracks budget goals.

## Stack
- Next.js 15 (App Router, TypeScript)
- SQLite via better-sqlite3 (data/pare.db)
- Recharts for charts
- Tailwind CSS 4 + shadcn/ui (brutalist theme — zero border-radius, monochrome, monospace headers)
- Python parser called via child_process for PDF ingestion (requires pdftotext / poppler)

## Design System
Brutalist bento aesthetic — monochrome chrome (1px borders, `--radius: 0`, JetBrains Mono ALL-CAPS headings / Geist body); colour reserved for DATA via `categoryColor()` / `PALETTE` (`lib/colors.ts`). **Full reference: [DESIGN.md](DESIGN.md)** — tokens, palette, the `@base-ui` (not radix) component patterns, goal-bar/radius conventions.

## Privacy
ALL financial data is PII. Never commit real transactions, PDFs, or the SQLite database. The data/ directory is gitignored. Only synthetic/redacted fixtures in tests.
**Tracked source ships a GENERIC taxonomy only** — `STARTER_RULES` (lib/db/categories.ts) and `lib/parser/categories.py` contain universal merchants/patterns, no personal merchants. The real, tuned keyword list lives in gitignored `data/seed-rules.json` (`loadSeedRules`), used as the seed source when present. Keep personal merchant names, the e-transfer handle, and absolute home paths OUT of tracked source (tests + helpers use synthetic data / `process.cwd()`-relative paths).

## Repo layers (open-core)
This is an open-core monorepo: `lib/`, `app/`, `components/`, `mcp/` are the AGPL open-source core; **`cloud/` is the proprietary hosted layer** (billing, ingest queue, metering, plans, hosted SimpleFIN cron); `internal/` is private notes/decisions (never shipped). **Decide layer placement BEFORE writing code**: anything deploy-target-specific, billing-related, or hosted-infra belongs in `cloud/` — putting it in `lib/` has required corrective relocation more than once. `lib/` must NEVER import from `cloud/`; only composition roots (routes, worker.ts) wire the two together. `npm run guard` checks that no private material leaks into the public surface.

## Git workflow
- **Never commit directly on `main`.** All work goes on a dedicated branch (Claude sessions: an isolated worktree under `.claude/worktrees/`), lands via PR, **squash-merge only**. Before any commit, confirm location: `git branch --show-current` + `git rev-parse --show-toplevel`.
- **This checkout is shared** — the user's terminal and multiple parallel Claude sessions write to it. Before branch switches, resets, or any working-tree surgery in the MAIN checkout, check for other active sessions; stage only files you touched, never `git add -A`/`git add .`.
- **Duplicate-PR guard:** before starting a feature, check open PRs/branches for the same scope (parallel sessions have shipped the same feature twice).
- **A merged PR is NOT a deploy.** `main` is detached from production; hosted ships via merge → `deploy/full-app` → manual workflow (Actions → "Deploy full app", dispatched on the `deploy/full-app` ref). The "Workers Builds" check on main PRs is un-required red noise — don't chase it.
- **`main` and `deploy/full-app` carry the SAME `wrangler.toml` and `worker.ts`.** They used to differ (main had a trimmed, no-data-plane config); that split drifted for 38 commits and broke a deploy merge, so it was collapsed. `deploy/full-app` is now a release POINTER, not a config variant — keep it a fast-forward of `main`. `wrangler.toml` + `worker.ts` are a matched pair (a handler or exported DO class needs its declaration in the config); change them together. `wrangler.example.toml` is the placeholder template for FORKS — the committed `wrangler.toml` holds pare.money's real resource ids.

## Pages (app/)
- `/` — **Public marketing landing** (app/page.tsx, `"use client"`): the signed-OUT homepage, NOT the app. Zero-scroll by design — `h-full flex-col overflow-hidden` (top bar / hero / feature strip), so it fits one viewport on desktop and mobile (the product-preview bento is `hidden md:flex` to keep phones zero-scroll). Brutalist: mono headline, 1px borders, earth-tone accents only. Primary CTA = waitlist email capture (POST /api/waitlist), secondary = Sign in (/login); header + footer link to the open-source GitHub repo (`REPO_URL`, inline `GithubMark` SVG since lucide-react dropped its brand icons). The right-hand bento shows STATIC illustrative figures (the page is public, can't read gated data). proxy.ts makes `/` public and redirects signed-IN visitors to `/dashboard`; the Sidebar hides itself on `/` (as it does on /login). Waitlist persists to the `waitlist` table (migration 005) via lib/db/waitlist.ts (`joinWaitlist` — INSERT OR IGNORE on a UNIQUE email, so a repeat returns `alreadyJoined` without revealing list membership).
- `/demo` — **Public product demo** (app/demo/page.tsx, no auth, no sidebar): the OVERVIEW bento rendered from CHECKED-IN synthetic `public/demo-data.json` (regenerate: `npm run gen:demo` = seed-demo.ts → gen-demo-json.ts; deterministic mulberry32). Static JSON works on BOTH deploy targets and offline; only month-anchored figures are included — today-relative surfaces (forecast, insights, safe-to-spend) would rot in a frozen snapshot, so they are deliberately absent. Linked from the landing hero ("SEE IT WITH SAMPLE DATA") and the /login footer. middleware: `/demo` is in PUBLIC_PATHS + WAITLIST_PUBLIC, and `demo-data.json` is in the matcher EXCLUSION list (keep in sync like the PWA surfaces). seed-demo.ts backfills account_kind + chequing closing balances post-generate (its INSERTs predate migrations 004/006 — without the backfill every spend chart reads empty on a fresh seed).
- `/dashboard` — Dashboard (app/dashboard/page.tsx): the signed-in app entry. The page is the shell + OVERVIEW/BY CATEGORY; the other tabs are extracted components under components/dashboard/ (networth-tab, forecast-tab, cashflow-tab + cashflow-sankey, income-tab, baseline-tab, month-review); the REVIEW tab header has a SHARE CARD dialog (components/dashboard/share-card.tsx): canvas-rendered 1080×1350 brutalist recap PNG — **privacy default = category percentages only** (no $ amounts, no merchants, ever), an explicit INCLUDE $ AMOUNTS toggle, download + navigator.share(files). Canvas attaches via CALLBACK ref, not an [open] effect — base-ui portals DialogContent after `open` flips, so an effect fires before the canvas exists; shared client formatters + CHART_TOOLTIP_STYLE live in lib/format.ts. OVERVIEW (monthly bar, category donut, totals, goals, top merchants) + BY CATEGORY + INCOME + BASELINE tabs. BASELINE tab (lib/db/baseline.ts, /api/summary?type=baseline&threshold=N): discretionary baseline = card spend with single charges ≥ threshold removed; total-vs-baseline bars, baseline-avg vs total-avg, $200/$300/$500 threshold control, and a transparent excluded-one-offs list. INCOME tab driven by lib/db/income.ts (income-vs-spend grouped bars, income-by-type donut, totals); income type derived from description (PEOPLE CENTER→Payroll, REMBOURS/REFUND→Tax refund, etc.). INCOME tab also shows net cashflow: per-month net bar chart (sage surplus / terracotta deficit) + NET THIS MONTH (with MoM delta) + PERIOD SURPLUS cards, computed client-side as income − fixed − variable for months with chequing data. Net DOES subtract rent: `getIncomeVsSpend` counts categorized chequing transfers (rent via the user rule on the e-transfer handle) as expenses; 'Rent / housing' + 'Phone / utilities' + chequing fees are the fixed bucket.
- CASHFLOW tab: money-flow Sankey (lib/db/cashflow.ts, /api/summary?type=cashflow&month=YYYY-MM) — income types → INCOME hub → top-8 spend categories + EVERYTHING ELSE + SAVED (deficit months get a FROM SAVINGS source node instead); ALL/per-month chips (only months with chequing data); MONEY IN/OUT/SAVED cards with savings rate. Below it, FORECAST for the current CALENDAR month (lib/db/forecast.ts, /api/summary?type=forecast): income projected from payroll only (one-off refunds excluded), fixed+variable from the MEDIAN of the last 3 complete months (median so a one-off-heavy month doesn't inflate it); switches to per-category pace mode (so-far ÷ days × days-in-month, CATEGORY PACE list vs typical) when the current month has ≥5 days of data. Sankey node labels are custom (renderFlowNode in components/dashboard/cashflow-sankey.tsx): two-line, `paintOrder: stroke` halo in var(--card) so they survive link crossings and dark mode; names truncated at 22 chars to fit the 135px side margins. Between Sankey and FORECAST sits the DAILY SPEND calendar heatmap (components/dashboard/calendar-heatmap.tsx, lib/db/heatmap.ts, /api/summary?type=heatmap): the spend-charts universe (`SPEND_WHERE`: card + manual cash — NOT the Sankey's), terracotta-alpha ramp anchored to the p95 of ALL days so months stay comparable while navigating, ←/→ month nav that also follows the tab's month chips (syncMonth prop), hover detail line, MONTH SPEND/BUSIEST DAY/SPEND DAYS stat cards, and a SPEND BY WEEKDAY row (avg per weekday over the WHOLE data range — no-spend days count as zeros, divided by weekday occurrences between first/last data date, not spend days; local-time date parse to avoid UTC weekday shift). Renders even without chequing data (below the empty-state card).
- FORECAST tab (lib/db/cashflowForecast.ts, /api/summary?type=cashflow_forecast): 30/60/90-day cash-flow forecast — the latest chequing `closing_balance` from `statements` (the balance-reconciled anchor; NULL anchor → "re-upload your latest chequing statement" empty state) projected forward daily. Disjoint buckets: payroll as discrete deposits (median of last ≤8 PEOPLE CENTER/PAYROLL deposits, cadence = median gap clamped 7–35d), the income.ts fixed bucket as ONE monthly event on the observed rent day, cadence-scheduled subscriptions from getSubscriptions() (fixed-category subs stay in fixed, `irregular` ones stay in discretionary), and discretionary = median variable MINUS scheduled subs, drained daily. Uncertainty band = ±1σ of monthly variable spend (last ≤6 complete months) × √(days/30.44). Engine always returns 90 daily points + per-horizon summaries; the client slices for the 30D/60D/90D toggle. UI: balance line (slate) + range Area band (dustyblue, `band: [low, high]`), YAxis domain auto (not 0-pinned), TODAY ReferenceLine when inside the window, END/LOWEST POINT/ANCHOR cards, ESTIMATE INPUTS + SCHEDULED IN WINDOW lists, explicit "estimate, not a promise / statements lag the calendar" framing with anchor staleness. One-off income deliberately excluded (consistent with forecast.ts); card spend modelled the day it happens, not when the card payment clears.
- NET WORTH tab (lib/db/networth.ts, /api/summary?type=net_worth, /api/networth GET/POST/PUT/DELETE): statement-cadence net worth. Statements carry `closing_balance` + `closing_date` (migration 004), extracted by `statement_meta()` in the parser — balances stored AS PRINTED (positive = owed for cards); `getNetWorth()` signs amex/cibc_visa negative, chequing positive. `manual_entries` table (name, kind asset|liability, amount, effective_date, note) holds non-statement items (investments, vehicle); re-adding the same name with a newer effective date updates its value — history builds the trend. Series = monthly points from first to last observation, latest balance per account/item carried forward (point-in-time by design, NOT live). `insertStatement` is an UPSERT by filename, so re-uploading a PDF backfills balances on pre-004 rows. Trend chart: stepAfter lines — net (currentColor, bold), assets (sage dashed), liabilities (terracotta dashed); BALANCES breakdown card (sage/terracotta dots by kind); MANUAL ENTRIES list with EDIT/REMOVE + ADD ENTRY dialog. **Properties fold in too** (Phase 2, `type: "property"` alongside `"statement"`/`"manual"`): each property becomes UP TO TWO lines — its `property_value_history` as an asset (one observation per dated entry, a real trend) and, if set, its mortgage as a liability — never netted into one combined equity figure, matching the never-net-a-statement-pair precedent. Timeline keys are namespaced `property:{id}:value` / `property:{id}:mortgage` (never the raw property name — a collision would silently merge two accounts' histories, the same hazard nicknames already guard against) with the property's name kept only as the display `label`. **The mortgage liability is anchored to `date_opened`, NOT `updated_at`** — `property_mortgages` is a strict upsert and `setMortgage()` bumps `updated_at` on every save, so anchoring to it would make the liability's date move forward on every edit and silently erase it from already-shown past months; `date_opened` stays stable across balance edits in the normal flow. Derivation is a pure function, `propertyNetWorthObservations()` in lib/db/networth.ts (no DB access, same "pure module" precedent as `lib/safe-to-spend.ts`). No hidden/closed filtering for properties yet (Phase 1 never added an `account_meta` equivalent) — every property with data always shows. Tests: `lib/repo/networth.test.ts`.
- SAFE TO SPEND hero (components/dashboard/safe-to-spend.tsx) sits above INSIGHTS: cushion = lowest projected balance from today to the first payroll AFTER the next rent event, derived client-side from the already-fetched cashflow_forecast by `deriveSafeToSpend` (lib/safe-to-spend.ts — PURE module, no DB/React imports; tests in lib/safe-to-spend.test.ts, `test:repo` glob). Status reuses the forecast's ±1σ band: short (balance<0, terracotta) / tight (point low<0≤balance, mustard) / clear (sage) / stale (today past the 90-day projection → upload nudge). Window falls back to 30 days when no payroll is detected. The self-host PDF upload path fires a "Forecast heads-up" push when a fresh statement leaves the status short.
- INSIGHTS panel sits at the top of the dashboard (above the tabs, always visible): local rule-based tips from `lib/db/insights.ts` (/api/summary?type=insights) — over/near-budget goals, MoM category moves, net surplus/deficit, large one-offs, forecast (projected net for the current calendar month + categories pacing ≥25%/$75 over typical when partial data exists), top category; severity-sorted & colour-dotted. All heuristics use the **latest data month**, not the calendar month — EXCEPT the forecast insight, which deliberately targets the current calendar month.
- `getCurrentProgress` (goals) and insights both use the latest month with spend data (statements lag the calendar). Don't switch these back to `new Date()` month — it would show empty current-month data.
- Layout uses a collapsible left **Sidebar** (`components/layout/navbar.tsx`, exports `Sidebar`) with lucide icons + dark-mode toggle; not a top navbar.
- `/transactions` — searchable/filterable table, SPEND/ALL flow tabs, pagination. **+ ADD CASH quick-add** (POST/DELETE /api/transactions, `insertManualTransaction` in lib/db/transactions.ts): source `manual`, account_kind `cash`, no statement row, dedup_key = `manual|<uuid>` (intentional duplicates always insert); the user's category pick is stored as a category_override over the rules-derived base so recategorizeAll never clobbers it; cash rows count in ALL spend charts + outflow (SPEND_WHERE/OUTFLOW_WHERE include kind 'cash'). Manual rows get a DELETE ENTRY button in the row dialog (statement-backed rows are refused); profile DATA HEALTH shows them as "CASH" with no staleness badge.
- `/recurring` — subscription/recurring detection + kill kit (lib/db/subscriptions.ts, /api/recurring GET/POST): merchants charged across 3+ months by stable-amount+cadence or known-merchant keyword; monthly/annual totals, frequency, variable-amount + double-bill flags. Kill kit: **price-change detection** (`detectPriceChange` — needs ≥5 charges, prior amounts within ±15% of their median AND last two within 2%, step ≥5%; a detected step suppresses the `variable` label and lets an otherwise-range-failed sub through detection), **lapse detection** (`lapsed` = no charge for >2× the cadence gap, measured against the newest SPEND date — never `new Date()`; lapsed subs stay listed but are excluded from `monthlyTotal` and from cashflowForecast's scheduled events), and **mark-to-cancel** (`subscription_marks` table, migration 008 — slug PK from merchantSlug, `monthly_cost` snapshot at mark time; survives the /api/data WIPE like rules/goals). POST /api/recurring `{action: mark|unmark, slug, …}` via `repo.subscriptions.mark/unmark`. UI: HIKE +N% (mustard) / 2×/MO? (terracotta) / GONE (sage) / "$X SINCE MARK" badges, MARK TO CANCEL / UNMARK row actions, CANCEL ↗ link from `lib/cancel-guides.ts` (generic merchants only + search fallback — same privacy rule as KNOWN_RECURRING), CANCEL LIST stat card (at-stake $/yr; bleed vs saved). Insights: price-hike (warn) + still-charging-after-mark (alert). Tests: lib/repo/subscription-marks.test.ts.
- `/upload` — drag-drop PDF statements **or `.ofx` / `.qfx` exports**; header copy reframes the friction as the feature ("the 30 seconds that keep your bank logins yours") and a WHERE TO GET YOUR STATEMENT accordion (components/upload/bank-guides.tsx, native `<details>`) gives per-bank download steps with parser-status badges (PDF TUNED for CIBC/Amex, PDF BETA · OFX SAFER for the scaffolds, OFX/QFX fallback for any bank — content mirrors the registry coverage; generic banks only) (the CSV-import button/route were REMOVED — they duplicated PDF data; see Data provenance). OFX/QFX is the dedup-safe successor to CSV: `lib/import/ofx.ts` (pure parser — handles both OFX 1.x SGML with unclosed leaf tags and OFX 2.x / QFX XML) → `lib/repo/insert-ofx.ts` (the OFX sibling of `insert-parsed.ts`). One **statement row per account** (NOT the `imports` table — OFX carries no categories, so it must flow through `recategorizeAll()`, which SKIPS `import_id` rows); `account_kind` set from the OFX account type (CHECKING→chequing, SAVINGS/MONEYMRKT→savings, CC block→card) so imported accounts light up every chart; dedup keyed on the bank-assigned `FITID` (`computeOfxDedupKey` = `sha256("ofx|source|fitId")`, namespaced per account, falls back to the positional key when FITID is absent) so re-importing an overlapping file is idempotent. Deposit-account `LEDGERBAL` → `closing_balance` (net-worth anchor); **card closing balances are left NULL** (issuer BALAMT sign is inconsistent — a deliberate follow-up). Self-host route branch only (`app/api/upload/route.ts` → `handleOfxImport`); the hosted PDF queue is untouched. Parser tests: `lib/import/ofx.test.ts` (in the `test:repo` glob). **SimpleFIN sync (opt-in, both targets):** the "OR: CONNECT A BANK" card below the drop zone (components/upload/simplefin-card.tsx) — the user pays SimpleFIN Bridge directly, pastes a one-time setup token, Pare claims it and pulls JSON via `lib/import/simplefin.ts` (pure client+adapter → the OFX import shape). The sync core is `lib/simplefin/sync.ts` (`runSimplefinSync` — everything deploy-specific injected: config store, account gate, notify, fetch), shared by the route (both modes) and the hosted cron; per-account statement filename `<source>.sync` so the balance anchor UPSERTs in place. **Dedup is CONTENT-positional (fitId left blank), NOT bridge-txn-id-keyed:** the live demo bridge regenerates ids per request (verified 2026-07-06 — id-keyed dedup would silently double the whole history every sync, the CSV-disaster class); incremental windows are floored to UTC midnight, backfill windows are strictly disjoint, and the backfill merge keeps the MAX same-content group per single window (so even a bridge that ignores date params can't multiply the backfill). Source = `simplefin_<slug>` **without** a kind suffix (dedup keys are source-namespaced; a kind change must never change the source) — `account_kind` is the user's one-time classification at connect (SimpleFIN has no type field) and is FROZEN once the account has synced. Connection state + access URL (a bearer secret — never log it): self-host in gitignored `data/simplefin.json` (lib/db/simplefin-config.ts — survives WIPE, stays out of `?format=backup`); hosted in the D1 `simplefin_integration` table (migration 0006, config as a JSON blob, same shape), both behind the `SimplefinConfigStore` interface (lib/simplefin/config-store.ts). `/api/simplefin`: GET status, POST claim/configure/sync/disconnect; `PARE_SIMPLEFIN_DISABLED=1` hides it. **Hosted gating (composition root only — lib/ never imports cloud/):** claim requires the `simplefin` plan Feature (Plus/Founder; 402 `{upgrade:true}` → the card renders an UPGRADE→/pricing tile via `upgradeRequired` on GET), and sync enforces the per-plan ACCOUNT cap per NEW source via `checkAccountLimit` — over-cap accounts are skipped with a "PLAN CAP:" notice in `errors`, never silently dropped; existing sources always re-sync (existing-first ordering). Auto-sync: `SimplefinAutoSync` on the dashboard fires `{action:"sync", auto:true}` and the server no-ops unless the last success is >20h old (1h failure cooldown; bridge quota ~24 req/day); hosted ALSO runs a daily Worker cron (`[triggers] crons` in wrangler.toml → worker.ts `scheduled` → cloud/simplefin/scheduled.ts) that scans D1 and runs the same core per due user — everything resolves off the `env` parameter (`resolvePlanFrom(db,…)`, `getRepoForUser(userId, env.USER_DATA)`); getCloudflareContext is NOT available in scheduled(), same as queue(). The cron re-checks the feature each run, so downgraded users stop syncing but keep the card visible to disconnect. Push notifications on fresh rows are self-host only (web-push is Node-only). First sync backfills 360 days in ≤90-day windows; `pending` rows are always skipped (they mutate on post). Bridge-reported errors MUST surface on the card (protocol requirement — how "reconnect at the bridge" MFA states reach the user). Tests: `lib/import/simplefin.test.ts` + `lib/simplefin/sync.test.ts` (adversarial bridge: unstable ids, window-ignoring backfill, plan-cap gate).
- `/categories` — rule CRUD grouped by category, override→rule suggestions, recategorize-on-add
- `/goals` — monthly limits, progress bars (green<80 / yellow<100 / red over), suggested limits from 6-mo avg
- `/properties` — real-estate tracking: address, mortgage (outstanding amount/date opened/rate/amortization years → auto-calculated P&I via the standard amortization formula in `lib/db/properties.ts`, user-overridable), recurring monthly expenses, type (primary/rental/vacation/other), and (rentals only) monthly rental income. NET VALUE = latest `property_value_history` entry − outstanding mortgage; NET INCOME (rentals) = rental income − (effective payment + expenses). Mortgage balance is a manually-updated snapshot, not auto-amortized — same point-in-time philosophy as statement-cadence net worth. Property value is a dated history (`property_value_history`, same shape as `manual_entries`) so appreciation builds a trend rather than overwriting a number. **Wired into Net Worth** (Phase 2, see the NET WORTH tab entry below) — property value and mortgage fold into `getNetWorth()`'s account list. Still NOT wired into Income/Cashflow/Forecast/Insights (tracked as a follow-up); no MCP tools yet either (every other domain has them — known, temporary gap). **Deviates from the rules/goals/account_meta wipe convention: property records ARE cleared by the `/api/data` WIPE**, not kept — flagged explicitly so this isn't "fixed" back by a future reader assuming it's an oversight. Full repo-seam domain (`PropertyRepo` in `lib/repo/types.ts`, `sqlite-repo.ts`, `repo-rpc.ts` catalogue) — works on both self-host and hosted. Tests: `lib/repo/properties.test.ts`.
- `/connect` — MCP setup instructions (server component, `force-dynamic`): computes the machine's absolute paths (`process.cwd()`, `process.execPath`, `PARE_DB_PATH`) per request so the Claude Code (`~/.claude.json`) and Claude Desktop config snippets are copy-paste ready; the Desktop variant wraps the command in `/bin/sh -c "cd …"` because its config has no `cwd` field (migrations resolve via cwd). Tool list (13 read / 11 write), example prompts, privacy note; `CopyBlock` (components/connect/copy-block.tsx) falls back to `execCommand("copy")` outside secure contexts.
- `/login` — full-screen auth gate (sidebar hides itself on this path). First run (no app_user row) shows CREATE YOUR PROFILE (name + password); afterwards a password sign-in. Redirect target comes from `?from=` but only same-app paths are followed.
- `/profile` — calm hierarchy, top→bottom: unboxed identity MASTHEAD (avatar, inline name edit, member-since · "All data local", SIGN OUT, and the **CLAUDE indicator** — dot + mono text linking /connect; hosted = live connector OAuth token via `getMcpConnection`, self-host = the stdio server's heartbeat file `data/mcp-heartbeat.json` written by `mcp/heartbeat.ts` on startup + every 10 min, surfaced as CONNECTED / LAST SEEN / CONNECT-nudge; `/api/profile` merges it at the composition root like SimpleFIN status, null = unknown → indicator hidden); then DATA HEALTH as the hero card; then (hosted) PLAN + STATEMENT INBOX side by side; then a quiet 3-up row: SECURITY card (password-changed date from `app_user.password_changed_at`, migration 003; change-password dialog; hosted-only DISCONNECT CLAUDE when the connector is linked → POST `/api/mcp-disconnect` revokes the user's OAuth tokens + consent rows via `revokeMcpConnection` — self-host has no grant to revoke, the route 404s) + exports + DANGER ZONE (red label/buttons, neutral border). Per-source status chips are colored dot + text, not bordered badges. DATA HEALTH panel from `lib/db/profile.ts` (`getDataHealth` via GET /api/auth `profile.health`): stat tiles, categorized-% + rule-count link, and per-source rows (last statement period, statement count, last txn, 12-month coverage strip, CURRENT/sage vs "Nd — UPLOAD"/terracotta badge at >40 days since last txn); exports + DANGER ZONE. **Account management (migration 009, `account_meta` table keyed by source — survives WIPE like rules/goals):** each per-source row has a MANAGE dialog (nickname / HIDE FROM CHARTS / MARK CLOSED via `lib/db/accounts.ts`, `/api/accounts` GET/POST, `repo.accounts`). **hidden** is enforced at the VIEW level — migration 009 adds a `WHERE source NOT IN (… hidden=1)` filter to `v_transactions`, so every chart/list/insight excludes it in one place; statements-side readers (net worth, forecast anchor) carry explicit NOT-IN filters; exports and data health read the BASE table so hidden rows are never dropped from exports (data health shows the row dimmed + HIDDEN). **closed** keeps history but suppresses staleness nudges (CLOSED badge), stops net-worth carry-forward after its last statement, and can no longer anchor the cash-flow forecast. Nickname overrides the derived `sourceLabel` (now in accounts.ts, re-used by profile.ts). `/api/data`: GET `?format=csv` (base-table CSV — NOT v_transactions, hidden accounts must export; columns incl. `id`, comma-joined `tags`, `reimbursement_status`) + `?format=json` (transactions+rules+splits+goals) go through **`getScopedRepo`** (`repo.transactions.exportAll()` + `splits.listAll()`, both new export reads that read the BASE table incl. hidden), so CSV+JSON work on **BOTH** targets (self-host file DB and the hosted DO-per-user backend); `?format=backup` (better-sqlite3 online `db.backup()` → single consolidated file, restorable by copying over the DB) is **SELF-HOST ONLY** — the hosted DO uses native `ctx.storage.sql` with no serialisable `.db` and no copy-over restore path, so the route 404s backup in hosted mode and the profile page hides the BACKUP DB button when `hosted`. DELETE with `{"confirm":"WIPE"}` wipes transactions+statements+overrides **and property records (properties/property_mortgages/property_expenses/property_value_history — a deliberate deviation from the rules/goals/account_meta survive-wipe convention, see `/properties`)** in one SQL transaction (**overrides first — FK to transactions(id)**), keeping rules/goals/app_user — still **self-host only** (reads the file DB directly; wipe-on-hosted is an open follow-up).

## Auth (single-user)
- **`middleware.ts`** (root; **Edge** runtime) gates every route except the public ones: `/` (marketing landing — but signed-IN visitors are redirected to `/dashboard`), `/login`, `/api/auth`, and `/api/waitlist` (PUBLIC_PATHS). Other pages redirect to `/login?from=…`, `/api/*` gets 401 JSON. It verifies a stateless HMAC session cookie (`pare_session`, 30d) via `lib/auth/session-token.ts` (WebCrypto HMAC-SHA256, runtime-agnostic — runs identically on Node 20+ and Cloudflare Workers). **Why `middleware.ts` and not `proxy.ts`:** Next 16 renamed `middleware`→`proxy`, but `proxy` is locked to the Node.js runtime, which `@opennextjs/cloudflare` cannot bundle ("Node.js middleware is not currently supported") — `cf:build`/`cf:deploy` fail. The `middleware.ts` convention stays on the Edge runtime, which OpenNext supports. The Edge runtime forbids `node:fs`/`node:crypto`, so the gate uses WebCrypto + an **env** secret (no file reads at the edge).
- Signing secret resolution (`lib/auth/session.ts`, the Node secret store imported by the API routes): **`process.env.PARE_AUTH_SECRET`** first — the canonical, runtime-agnostic source and the ONLY value the Edge middleware can read; the Node routes read the SAME value, so cookies signed there verify in the middleware. Falls back to the generated, rotating gitignored `data/auth-secret` file (zero-config local Node use; the Edge middleware can't see it, so without the env var the middleware treats everyone as signed-out). **Self-host: set `PARE_AUTH_SECRET`** for page gating. **Password change rotates the file secret** (file mode only — a no-op under an env secret, which is rotated by changing the env var + restart), invalidating all sessions; the change-password handler re-issues the caller's cookie. `verifySessionToken`/`createSessionToken` are now **async** (WebCrypto). The session modules must NEVER import the DB (auth hot path; the Edge middleware can't use better-sqlite3 anyway).
- Password: scrypt (`scrypt:N:r:p:salt:hash` in `app_user.password_hash`, single row id=1, migration 002). Login/change-password failures sleep 500ms.
- `/api/auth` actions: `setup` (409 once configured), `login`, `logout`, `update_profile`, `change_password`; GET returns `{configured, authenticated, profile?}`. Auth is enforced ONLY in the proxy for the other API routes — if the matcher ever changes or server actions are added, add per-route checks (see Next data-security guide).
- MCP server + Python parser bypass HTTP entirely, so they're unaffected by auth.

## Commands
- `npm run dev` — start dev server (localhost:3000)
- `npx next build` — type-check + build (run before declaring done)
- `npm test` — parser regression suite (stdlib unittest, synthetic fixtures in `tests/`, no PDFs/real data). Run after touching `lib/parser/*.py` or `categories.py`. Tests monkeypatch `parse_statements.text` and assert reconciliation/flows/vocab/date-inference/categorizer behaviour.
- `npm run test:e2e` — Playwright E2E suite (`e2e/`, self-host mode only). Boots its OWN fully isolated server on :3111 (scratch DB **and scratch cwd** — user-rules.json/seed-rules.json/auth-secret resolve from `process.cwd()`, so a repo-root server would touch personal data). Next 16 allows one dev server per project: stop `npm run dev` first, or use `E2E_PROD=1` (production build + `next start`, no dev lock). Conventions + gotchas: `.claude/skills/playwright-e2e/SKILL.md`. Fixtures are synthetic OFX only (`e2e/fixtures/`), never PDFs/real data.
- `npm run mcp` — start the finance MCP server (stdio). Reuses `lib/db`; reads/writes `data/pare.db`. 24 tools (13 read / 11 write, incl. tags + reimbursements). Set `PARE_DB_PATH` to the absolute DB path when launched by an MCP client (cwd is unknown). Smoke test: `npx tsx mcp/test-client.ts`. See `mcp/README.md` for tools + Claude Code config. db.ts honors `PARE_DB_PATH` (falls back to `<cwd>/data/pare.db`).
- `python3 lib/parser/parse_statements.py <dir> <out.csv>` — run PDF parser standalone (or `<dir> --json` for JSON)

## Key Patterns
- API routes in app/api/*/route.ts use Web Request/Response API (Next 15, `request.formData()` native)
- SQLite singleton in lib/db.ts (WAL mode, foreign keys ON); query layers in lib/db/*.ts
- Migrations in lib/db/migrations/*.sql, applied by lib/db/migrate.ts at first getDb()
- Category rules seeded from gitignored `data/seed-rules.json` (personal taxonomy) if present, else generic `STARTER_RULES` in lib/db/categories.ts; first-match-wins. The Python parser ships generic too, so `/api/upload` calls `recategorizeAll()` after inserts to apply the DB's full rules to new transactions.
- Dedup: SHA-256 hash of (source|txn_date|description|amount|seq), INSERT OR IGNORE
- Spend charts count `SPEND_WHERE` (lib/db/account-kinds.ts): flow='spend' AND account_kind IN ('card','cash') — card purchases + manual cash rows (v_transactions view resolves overrides)

## PWA
Installable PWA (Add to Home Screen, iOS + Android). Pieces: `app/manifest.ts` (manifest incl. Android `share_target` POSTing PDFs/OFX to `/upload`), `public/sw.js` (hand-rolled SW, no workbox: cache-first for hashed assets, network-first + cache fallback for navigations and GET `/api/*` — offline read-through; `/api/auth` never cached). Cache names are `pare-{static,data}-v2-<buildId>`: the SW reads its build id from its own url query (`RegisterSW` registers `/sw.js?v=NEXT_PUBLIC_BUILD_ID`, wired in next.config.ts), so **every deploy gets fresh cache names and `activate` evicts the previous build's cached chunks** — the fix for the installed-PWA "failed to load chunk" error. Bump the `v2` epoch only when the caching LOGIC changes (build id already busts per-deploy). Post-deploy chunk mismatches that still slip through are caught client-side by `ChunkGuard` (in register-sw.tsx) + the `isChunkLoadError` branch in `app/error.tsx`/`app/global-error.tsx`, which clear caches and reload onto the new build (`lib/chunk-recovery.ts`, loop-guarded). `components/pwa/register-sw.tsx` (`RegisterSW` prod-only + `ChunkGuard` + `OfflineBanner`, all in the root layout), `app/offline/page.tsx` (precached fallback). Share-target flow: SW intercepts POST `/upload` → stashes files in the `pare-share-intake` cache → 303 to `/upload?share-target=1` → the page reads the stash and reuses the normal client upload path. Install prompt (`beforeinstallprompt`) is offered on /upload only after a successful upload. **Web push (self-host/Node only):** `/api/push` GET (VAPID public key) / POST subscribe / DELETE; subscriptions in `push_subscriptions` (migration 007); sender `lib/push/webpush.ts` (`web-push` pkg — VAPID keys from `PARE_VAPID_{PUBLIC,PRIVATE}_KEY` env else generated gitignored `data/vapid.json`; 404/410 sends auto-prune the subscription). Parse-complete push fires after the self-host `/api/upload` PDF path (fire-and-forget). Opt-in card ("GET PARSE ALERTS") on /upload after first success; the subscribe POST sends a confirmation push as the end-to-end test. Hosted push = 501 until the Workers queue consumer gets a WebCrypto sender (follow-up). Icons are generated — edit `scripts/generate-pwa-icons.mjs` (pear mark master) and re-run it; never hand-edit `public/icon-*.png` / `app/icon.svg` / `app/apple-icon.png` / `public/favicon.ico`. **The middleware matcher excludes all PWA surfaces** (manifest.webmanifest, sw.js, icons, /offline) — keep that list in sync when renaming any of them.

## Gotchas
- **shadcn here uses @base-ui/react, NOT @radix-ui.** DialogTrigger has no `asChild` — render the trigger as its own styled element. Tabs use `@base-ui/react/tabs` and support a `line` variant + vertical orientation.
- **Dev server caches the SQLite connection in memory.** After deleting data/pare.db or changing schema/migrations, restart `npm run dev` — the in-process singleton won't pick up a fresh file otherwise.
- **CSV from Start/ has Windows \r line endings.** Strip with `.replace(/\r/g, "")` before parsing or the trailing `flow` value fails the CHECK constraint and rows get silently dropped by INSERT OR IGNORE.
- Recharts Tooltip `formatter` types want `(value) => ...` then `Number(value)` — typing the param as `number` fails the build.

## Parser (lib/parser/parse_statements.py)
**Registry routing.** `main()` routes via the registry (`registry.py`) driven by the orchestrator (`orchestrator.py`), NOT an if/elif — each parser registers with a `matches` predicate + integer `priority`; `registry.select()` returns the lowest-priority match. `register_builtins` wires the three VERIFIED parsers (CIBC Visa 10 → CIBC chequing 20 → **Amex fallback 90**); `register_scaffolds` wires the scaffolded banks from `_SCAFFOLD_BANKS` (priorities 30–80, so they slot between chequing and Amex). **Order matters:** chequing PDFs contain the literal "American Express" (Amex card-payment lines), so Amex stays the lowest-priority fallback; for a brand with both chequing AND savings (Tangerine, Wealthsimple) the SAVINGS handler is prioritised first (its detector keys on a "Savings" marker). Every parser (built-in + scaffold) registers a `meta` fn, and the orchestrator prefers `parser.meta(path)` — the built-ins' per-source meta fns (`_amex_meta` etc., which also capture `opening_balance` for `verify.py`) and detectors (`_detect_*`) are shared with `statement_meta()`, which remains only as a thin standalone router over the same trio (one source of truth). Run standalone: `python3 lib/parser/parse_statements.py <dir> --json`. **`--json` outputs `{transactions: [...], statements: [...]}`**: `statements` is per-file metadata — source/account/period/filename + `closing_balance` (as printed) and `closing_date` (`period_end()`, handles all three period shapes incl. Visa's full month names). CSV mode is unchanged (transactions only).

**Coverage status.** Only **CIBC (Visa + chequing) and Amex are tuned against REAL PDFs.** Everything else — **RBC, TD, Scotia, BMO (card + chequing), Tangerine (chequing + savings), Wealthsimple (cash + savings)** — is a **SCAFFOLD**: regexes reconstructed from documented layouts, covered by *synthetic* fixtures only (no real samples). They WILL need a regex pass on the first real upload of each. The ledger engine fails safe (skips + logs rows that don't reconcile → missing rows, never corrupt totals); **card parsers have no checksum and are the riskiest surface.** New `source` strings light up the charts automatically via the `<bank>_<kind>` naming convention in `sourceToKind()` (lib/db/account-kinds.ts) — no per-bank TS edit; `profile.ts` labels derive the same way (`sourceLabel`).

- **Amex:** period = the CLOSING date (header value line has Opening + Closing; take the 2nd). Year = closing year; only Dec txns on a Jan-closing statement roll back a year. Bespoke parser (predates the shared card engine).
- **CIBC Visa:** splits description from CIBC's "Spend Categories" column using the REAL fixed vocabulary (Retail and Grocery, Restaurants, Transportation, Hotel and Travel, Home and Office, Health and Education, Professional and Financial Services, Personal and Household Expenses, Entertainment, Foreign Currency Transactions). Getting this list wrong silently skips rows. Bespoke parser.
- **Card engine (`_parse_card` + `CardProfile`) — RBC/TD/Scotia/BMO scaffolds:** two-date rows like Amex (`MON DD MON DD DESC $AMT`); credits (`-$X`/trailing `CR`) → `payment` (if "PAYMENT" in desc) else `income` (refund); `NEW BALANCE`/`TOTAL BALANCE` → closing-balance meta. RBC gates on its `Transaction/Posting/Description` header; TD/Scotia/BMO run ungated and rely on the two-date row shape.
- **Ledger engine (`_walk_ledger` + `LedgerProfile`) — CIBC chequing + all scaffold chequing/savings:** `_walk_ledger_text(profile, t)` is the shared core; CIBC chequing is the `CIBC_LEDGER` profile over it (its `_walk_chequing` / `_walk_chequing_text` / `chequing_report()` names kept as thin CIBC-bound shims — `verify.py` still re-walks via `_walk_chequing_text`). Direction comes from balance reconciliation (prev ± amount == printed balance), NOT column offsets. Continuation lines fold onto the preceding txn. Amounts stored positive; flow encodes direction; category stays 'Banking'. Verify any ledger statement with `ledger_report(path, profile)` (`chequing_report` = the CIBC wrapper) — ties parsed inflow/outflow/closing to the summary box.
- **MONEY_TOKEN** has a `(?!\d)` lookahead + an FX_NOTE guard so exchange-rate lines ("35.00 USD @ 1.4329") aren't mistaken for transactions.

## Data provenance
Source of truth = real PDF statements kept in the gitignored repo root `..` (Amex, CIBC Visa, CIBC chequing); the DB is wiped and re-ingested from these. Any older `Start/transactions.csv` is superseded. **The CSV-import button and `/api/upload/csv-import` route were removed** — importing the CSV used period-start dates (PDFs use real per-txn dates → different dedup keys → silent duplicates that doubled every metric). PDFs **or OFX/QFX** — the latter is the dedup-safe CSV replacement (FITID-keyed; see the `/upload` entry above). (The dormant inline `csv` branch in `app/api/upload/route.ts` has been deleted.) **This removal is ONLY the upload-drop-zone CSV path** — do NOT read it as "Pare can't import CSV." A separate, LIVE cross-app migration importer exists (`/switch` → `/api/import` + `/api/import/commit`, `components/switch/import-wizard.tsx`, presets in `lib/import/presets.ts`, PR #39): it DOES read Monarch/Mint/YNAB CSV exports (auto-detected, dedup-safe via `lib/import/overlap.ts`, preview→commit, on both deploy targets via `getScopedRepo`). So: the `/upload` drop zone is PDF/OFX/QFX only; competitor-CSV migration lives at `/switch`. Rent is a recurring e-transfer; tag it via an in-app category rule on the recipient (keeps the personal handle out of tracked source).

## Recategorization
- `recategorizeAll()` (lib/db/categories.ts) re-applies category_rules (first-match-wins), skipping manual overrides. Card/cash rows: full rules, fallback 'Other / uncategorized'. **Deposit rows** (`isDepositKind` — chequing/savings/investment, `DEPOSIT_KINDS` in account-kinds.ts): **debit purchases (flow=spend)** get any rule (fallback 'Banking'); **transfers (flow=transfer)** get ONLY user-defined categories — seeded card-merchant rules are excluded so a location like "TELUS Garden Banking Ctr" doesn't false-match Phone/utilities; income/payment/fee_interest are never reclassified. This is how rent (a transfer) gets tagged 'Rent / housing' via an in-app rule on the private e-transfer handle (never hardcode the handle in source). **Key the deposit contract off `isDepositKind`, NOT `account_kind === 'chequing'`** — savings/investment sources (SimpleFIN, OFX MONEYMRKT) are deposit accounts too; the old literal check dropped their unmatched spend into 'Other / uncategorized' and reclassified their income.

**User rules survive DB wipes.** addRule/deleteRule also persist to `data/user-rules.json` (gitignored), and `seedCategoryRules()` restores them on top of the built-in SEED_RULES whenever a fresh DB is seeded. So after a wipe + re-ingest the rent rule (and any other custom rule) is auto-restored — no manual re-add. The private handle lives only in that gitignored file, never in tracked source. (Built-in rule *deletions* are not persisted — a wipe brings seeded rules back.)
- The CSV import calls it automatically (the seed CSV's categories came from the OLD buggy parser).
- Exposed as POST /api/categories `{action:"recategorize_all"}` and the "RECATEGORIZE ALL" button on /categories.
- **Rules import** (`importRules` in lib/db/categories.ts; POST /api/categories `{action:"import_rules", rules|category_rules}` → upsert-by-keyword then recategorizeAll; "IMPORT RULES" button on /categories). Accepts a bare rules array OR a full `/api/data?format=json` export (reads `category_rules`). Last-wins on keyword collision; blanks skipped; persists to user-rules.json (one write) so it survives a wipe. **Why it exists:** the tuned `data/seed-rules.json` taxonomy is gitignored and never ships in the Worker bundle, so a fresh HOSTED account only has the generic `STARTER_RULES` (loadSeedRules → null) — import lets a user carry their self-host rules over. Tests: lib/repo/rules-import.test.ts (runs over DoBackend = the hosted path). NOTE: `test:repo` now sets `PARE_DATA_DIR` to a temp dir so the suite never reads/writes the real `data/` (user-rules.json / seed-rules.json); the data dir honours `PARE_DATA_DIR` else `<cwd>/data`.
- **Known limitation:** the seed Start/transactions.csv has CIBC Visa descriptions the old parser
  truncated (e.g. "BREAKING", "BAR", "ALBERNI"). The full merchant name is gone, so ~12 rows stay
  "Other / uncategorized" and can't be rule-matched. Re-uploading the actual CIBC PDFs through the
  fixed parser yields full descriptions and resolves them. Do NOT add short keyword rules to catch
  these — that reintroduces the substring-collision class of bug we just fixed.

## UI components from ChromaDB
InputGroup (lib/components/ui/input-group.tsx) and the Tabs line variant were adapted from chroma-core/chroma sample_apps/movies. Prefer checking that repo for new component patterns. See design-aesthetic memory.
